/* 
let ch = prompt("Введите количество копеек (1-99): ", 5);

if (ch >= 1 && ch <= 99) {
    if (ch % 100 >= 11 && ch % 100 <= 14) {
        alert(`${ch} копеек`);
    
    } else if (ch % 10 == 1) {
        alert(`${ch} копейка`);

    } else if (ch % 10 >= 2 && ch % 10 <= 4) { 
        alert(`${ch} копейки`);

    } else {
        alert(`${ch} копеек`);
    }
} else { 
    alert("Недопустимое значение");
} */

console.log((2,5 - 2) * 2);
